# CLAUDE.md

このリポジトリで作業するときに、最初に読んでください。

## これは何か

2026年8月18日〜19日の家族旅行（大阪 → 鳥取 → 島根 → 広島・宮島）で、
車内のスマホから開くための静的サイトです。家族3人が閲覧するだけで、編集はしません。

**実際に旅行で使うものです。** 出発日が決まっているので、動かなくなる変更は避けてください。

## 構成

```
index.html      本体。HTML・CSS・JS がすべてこの1ファイルに入っています
sw.js           Service Worker。オフライン表示用
manifest.json   ホーム画面に追加したときの設定
icon.png        アプリアイコン（512x512）
README.md       人間向けの説明
```

外部ライブラリ・フレームワーク・ビルドツールは一切使っていません。
`npm install` するものはなく、`package.json` もありません。

## やってほしいこと

このディレクトリをGitHubリポジトリにして、VercelかGitHub Pagesで公開する。

- Vercel の場合：フレームワークプリセットは **Other**。ビルドコマンドは空。出力ディレクトリはルート
- GitHub Pages の場合：Settings → Pages → Source を `main` のルートに

どちらもhttpsで配信されます。Service Workerはhttpsでのみ動くので、これが条件です。

## やらないでほしいこと

- **ビルド環境を作らないでください。** Vite、webpack、npm scripts などは不要です。
  素のHTMLとして配信されることが前提の設計です
- **ファイルを分割しないでください。** CSSやJSを別ファイルに切り出すと、
  `sw.js` のキャッシュ対象リスト（`ASSETS`）も直す必要が出て、事故のもとになります
- **CDNからライブラリを読み込まないでください。** 電波の届かない山間部で使うため、
  外部依存をゼロにしてあります

## 落とし穴（重要）

### index.html を編集したら sw.js のバージョンを上げる

`sw.js` の1行目付近にこれがあります。

```js
const CACHE = "sanin-miyajima-v1";
```

Service Workerは古いHTMLを端末にキャッシュし続けるため、
**index.html を変更したら必ずこの数字を上げてください**（`-v2`, `-v3` …）。
上げないと、すでにサイトを開いた家族の端末では内容が更新されません。

編集とバージョン上げは必ずセットで行ってください。

### localStorage を消さない

持ち物のチェック状態と、走行タブの「乗った」時刻を localStorage に保存しています。
キーは `pack:0`〜`pack:7` と `leg:L1`〜`leg:L5` です。
キー名を変えると、家族が入れたチェックが消えます。

## データの場所

`index.html` の `<script>` 内、先頭にまとまっています。

| 定数 | 中身 |
|---|---|
| `DAYS` | 1日目・2日目の旅程。`t` が時刻、`n` が予定名、`m` が補足、`w` が警告 |
| `LEGS` | 高速道路の区間とSA/PA。`m` は「そのICで乗ってからの経過分」 |
| `PACK` | 持ち物リスト |
| `TRIP` | 旅行日（`{1:"2026-08-18", 2:"2026-08-19"}`）。この日付で「今日は何日目か」を判定 |

内容の修正はこの4つを直せば済むようにしてあります。HTMLの構造をいじる必要はありません。

## 未完了の項目

- **宿の電話番号**が暫定です。`index.html` 内の `tel:05030331039` と、
  その隣の表示文字列の2か所。予約サイト経由の転送番号の可能性があるため、
  公式サイト（https://kintanosato.com/）の直通番号に差し替えたい。
  該当箇所にはHTMLコメントを入れてあります

## 動作確認

ローカルで見るときは、`file://` で開くとService Workerが動きません。簡易サーバーを使ってください。

```
python3 -m http.server 8000
```

確認したいポイント：

- 画面下の4タブ（予定・走行・情報・持ち物）が切り替わる
- 「走行」タブで「乗った」を押すと経過時間が動き、次のSA/PAに「あと◯分」が出る
- 「持ち物」のチェックがリロード後も残る
- スマホ幅（375px程度）で崩れない

「予定」タブの現在地ハイライトは、実際の日付が旅行日でないと出ません。
確認したい場合は `TRIP` の日付を今日に変えて試し、**必ず元に戻してください**。
